import styled from "styled-components";

import { FlexRow } from "src/styles/utils";

const DataTabToolbar = styled(FlexRow)`
  justify-content: space-between;
  gap: 30px;
  width: 100%;
`;

export default DataTabToolbar;
